#Ordered Dictionary keeps track of the order of indexes as they are appended. This can be used to keep the elements in an order.
#Output in list
import numpy as np
from collections import OrderedDict 
x=np.array([16, 3, 5, 17, 2, 4, 1])
x = list(x)
x_dct = {x[i]:i for i in range(len(x))}
x_dct = OrderedDict(x_dct)
x_dct.values()
lst=[]
for i in sorted(x_dct):
  lst.append(i)
#x_dct
lst
